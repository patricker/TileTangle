# Playground Assets

Drop custom textures (logos, favicons, board overlays) into this folder. Godot treats everything under `res://assets/` as project resources, so new images are imported automatically with the default settings when you open the project.

Reference them from the playground scene via the standard resource picker (look under `res://assets/`). No additional build tooling or bundling is required.
