[workspace]
members = [
    "engine",
    "engine_ffi",
    "wasm",
    "bindings/python",
    "tools/dict-build",
]
resolver = "2"

[workspace.package]
edition = "2024"

[workspace.metadata]
ci = { rust_toolchain = "stable" }
